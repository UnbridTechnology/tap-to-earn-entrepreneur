import { networkData as esData } from "./es";
import { networkData as enData } from "./en";
import { networkData as ptData } from "./pt";

export const NetworkTranslation = Object.freeze({
    es: esData,
    en: enData,
    pt: ptData
});