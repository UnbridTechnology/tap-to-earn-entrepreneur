import { profileData as esData } from "./es";
import { profileData as enData } from "./en";
import { profileData as ptData } from "./pt";

export const ProfileTranslation = Object.freeze({
    es: esData,
    en: enData,
    pt: ptData
});