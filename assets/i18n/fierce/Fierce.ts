import { fierceData as esData } from "./es";
import { fierceData as enData } from "./en";
import { fierceData as ptData } from "./pt";

export const FierceTranslation = Object.freeze({
    es: esData,
    en: enData,
    pt: ptData
});