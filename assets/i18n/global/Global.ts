import { globalData as esData } from "./es";
import { globalData as enData } from "./en";
import { globalData as ptData } from "./pt";

export const GlobalTranslation = Object.freeze({
    es: esData,
    en: enData,
    pt: ptData
});