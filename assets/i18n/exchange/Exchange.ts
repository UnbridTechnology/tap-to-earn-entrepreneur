import { exchangeData as esData } from "./es";
import { exchangeData as enData } from "./en";
import { exchangeData as ptData } from "./pt";

export const ExchangeTranslation = Object.freeze({
    es: esData,
    en: enData,
    pt: ptData
});